<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8" />
  <title>{{name}} - Attribution Report</title>
  <style>
    body { font-family: Arial; padding: 30px; color: #222; }
    h1, h2 { margin-top: 40px; }
    table { width: 100%; border-collapse: collapse; margin-top: 10px; }
    td, th { padding: 6px; border: 1px solid #ccc; text-align: left; }
  </style>
</head>
<body>
  <h1>Offline Attribution Report</h1>
  <p><strong>Campaign:</strong> {{name}}</p>
  <p><strong>Channel:</strong> {{channel.name}} | <strong>Region:</strong> {{region.name}}</p>

  <h2>Promo Code Performance</h2>
  <table>
    <tr><th>Code</th><th>Uses</th></tr>
    {{#each promoCodes}}
      <tr><td>{{code}}</td><td>{{uses}}</td></tr>
    {{/each}}
  </table>

  <h2>KPIs</h2>
  <ul>
    <li>Conversions: {{kPI.attributedConversions}}</li>
    <li>Revenue: ${{kPI.attributedRevenue}}</li>
    <li>CPA: ${{kPI.averageCPA}}</li>
    <li>ROI: {{kPI.overallROI}}x</li>
    <li>Lift: {{kPI.incrementalLift}}%</li>
  </ul>

  <h2>Funnel Steps</h2>
  <ol>
    {{#each funnelSteps}}
      <li>{{step}}</li>
    {{/each}}
  </ol>
</body>
</html>
